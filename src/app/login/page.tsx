"use client";
import BonfireLogo from "@/components/BonfireLogo";
import Link from "next/link"; import { FormEvent, useState } from "react"; import { useRouter } from "next/navigation"; import { isSupabaseConfigured, supabase } from "@/lib/supabase";
export default function LoginPage(){const router=useRouter();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[message,setMessage]=useState("");const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(!isSupabaseConfigured){setMessage("Configure o arquivo .env.local antes de entrar.");return}setLoading(true);const{error}=await supabase.auth.signInWithPassword({email,password});setLoading(false);if(error){setMessage(error.message);return}router.push("/");router.refresh()}return <main className="flex min-h-screen items-center justify-center bg-[#11100f] px-6"><section className="w-full max-w-md"><div className="mb-8 text-center"><BonfireLogo large /><h1 className="mt-4 text-4xl font-black">Bonfire</h1><p className="text-[#b9aaa0]">Todo o IFC em volta da fogueira!</p></div><form onSubmit={submit} className="space-y-4 rounded-2xl border border-white/10 bg-white/[.045] p-6"><label className="block text-sm text-[#b9aaa0]">E-mail<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-lg border border-white/10 bg-black/20 px-4 py-3 text-white"/></label><label className="block text-sm text-[#b9aaa0]">Senha<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-lg border border-white/10 bg-black/20 px-4 py-3 text-white"/></label>{message&&<p className="rounded-lg border border-red-400/30 bg-red-400/10 p-3 text-sm text-red-200">{message}</p>}<button disabled={loading} className="w-full rounded-full bg-[#ff8a3d] px-5 py-3 font-bold text-[#21140e]">{loading?"Entrando...":"Entrar"}</button><Link href="/esqueci-senha" className="block text-center text-sm text-[#ffd19a]">Esqueci minha senha</Link><Link href="/cadastro" className="block text-center text-sm text-[#ffd19a]">Criar conta</Link></form></section></main>}
